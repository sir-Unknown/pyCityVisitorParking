# Changelog

## Unreleased

## 0.4.0

- Require `username` for login (no legacy identifier alias).
- Default `api_uri` to `/DVSWebAPI/api/login` when omitted.

## 0.2.0

- Initial DVS Portal provider implementation.
